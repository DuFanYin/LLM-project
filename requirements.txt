# Qwen2.5 0.7B download & inference
transformers>=4.40.0
accelerate>=0.27.0
torch>=2.0.0

# LoRA fine-tuning
peft>=0.10.0
datasets>=2.18.0
bitsandbytes>=0.43.0  # optional, for 4-bit quantized LoRA

# Math dataset (Hugging Face)
huggingface_hub>=0.22.0

# RAG / tool-calling (no extra deps; calculator is external)
