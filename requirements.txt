# Core: model load & inference
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.27.0

# LoRA fine-tuning
peft>=0.10.0

# Dataset load & tokenization
datasets>=2.18.0
huggingface_hub>=0.22.0

# Optional: 4-bit / 8-bit quantization (required only for load_in_4bit=True or QLoRA)
# pip install bitsandbytes  if you use QLoRA
bitsandbytes>=0.43.0
